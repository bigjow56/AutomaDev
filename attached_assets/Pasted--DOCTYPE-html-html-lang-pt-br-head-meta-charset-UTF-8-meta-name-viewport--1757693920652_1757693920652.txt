<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AutomaDev - Hero Section com Logo Background</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', sans-serif;
            overflow-x: hidden;
        }

        .hero-section {
            height: 100vh;
            background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        /* Logo Neural Network Background */
        .neural-background {
            position: absolute;
            width: 400px;
            height: 400px;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            opacity: 0.15;
            z-index: 1;
        }

        .neural-network {
            width: 100%;
            height: 100%;
            position: relative;
        }

        .node {
            position: absolute;
            width: 16px;
            height: 16px;
            background: #00ff88;
            border-radius: 50%;
            box-shadow: 0 0 20px #00ff88;
            animation: nodeGlow 3s ease-in-out infinite;
        }

        /* Posicionamento dos nós da rede neural */
        .node:nth-child(1) { top: 50px; left: 50px; animation-delay: 0s; }
        .node:nth-child(2) { top: 50px; right: 50px; animation-delay: 0.4s; }
        .node:nth-child(3) { top: 50%; left: 25%; transform: translateY(-50%); animation-delay: 0.8s; }
        .node:nth-child(4) { top: 50%; left: 50%; transform: translate(-50%, -50%); animation-delay: 1.2s; }
        .node:nth-child(5) { top: 50%; right: 25%; transform: translateY(-50%); animation-delay: 1.6s; }
        .node:nth-child(6) { bottom: 50px; left: 50px; animation-delay: 2s; }
        .node:nth-child(7) { bottom: 50px; right: 50px; animation-delay: 2.4s; }
        .node:nth-child(8) { top: 30%; left: 50%; transform: translate(-50%, -50%); animation-delay: 0.6s; }
        .node:nth-child(9) { bottom: 30%; left: 50%; transform: translate(-50%, 50%); animation-delay: 1.8s; }

        .connection {
            position: absolute;
            height: 2px;
            background: linear-gradient(90deg, transparent, #00ff88, transparent);
            opacity: 0.4;
            animation: dataFlow 4s linear infinite;
        }

        /* Conexões da rede neural */
        .conn-1 { top: 58px; left: 66px; width: 268px; transform: rotate(0deg); animation-delay: 0s; }
        .conn-2 { top: 120px; left: 140px; width: 180px; transform: rotate(35deg); animation-delay: 0.5s; }
        .conn-3 { top: 180px; left: 100px; width: 200px; transform: rotate(-15deg); animation-delay: 1s; }
        .conn-4 { top: 240px; left: 140px; width: 180px; transform: rotate(-35deg); animation-delay: 1.5s; }
        .conn-5 { bottom: 58px; left: 66px; width: 268px; animation-delay: 2s; }
        .conn-6 { top: 50%; left: 66px; width: 120px; transform: translateY(-50%) rotate(90deg); animation-delay: 2.5s; }
        .conn-7 { top: 50%; right: 66px; width: 120px; transform: translateY(-50%) rotate(90deg); animation-delay: 3s; }
        .conn-8 { top: 150px; left: 200px; width: 100px; transform: rotate(45deg); animation-delay: 0.8s; }

        /* Conteúdo do Hero */
        .hero-content {
            text-align: center;
            z-index: 2;
            position: relative;
            max-width: 800px;
            padding: 0 2rem;
        }

        .hero-title {
            font-size: 4rem;
            font-weight: 800;
            color: white;
            margin-bottom: 1.5rem;
            background: linear-gradient(45deg, #00ff88, #00d4ff, #8b5cf6);
            background-size: 300% 300%;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: gradientShift 4s ease-in-out infinite;
        }

        .hero-subtitle {
            font-size: 1.5rem;
            color: #94a3b8;
            margin-bottom: 2rem;
            font-weight: 300;
        }

        .hero-description {
            font-size: 1.2rem;
            color: #cbd5e1;
            line-height: 1.6;
            margin-bottom: 3rem;
        }

        .cta-button {
            background: linear-gradient(45deg, #00ff88, #00d4ff);
            color: #0f172a;
            padding: 1rem 2.5rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 255, 136, 0.3);
        }

        /* Partículas flutuantes */
        .floating-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
        }

        .particle {
            position: absolute;
            width: 4px;
            height: 4px;
            background: #00ff88;
            border-radius: 50%;
            opacity: 0.3;
            animation: particleFloat 8s linear infinite;
        }

        .particle:nth-child(1) { left: 10%; animation-delay: 0s; }
        .particle:nth-child(2) { left: 20%; animation-delay: 1s; }
        .particle:nth-child(3) { left: 30%; animation-delay: 2s; }
        .particle:nth-child(4) { left: 40%; animation-delay: 3s; }
        .particle:nth-child(5) { left: 50%; animation-delay: 4s; }
        .particle:nth-child(6) { left: 60%; animation-delay: 5s; }
        .particle:nth-child(7) { left: 70%; animation-delay: 6s; }
        .particle:nth-child(8) { left: 80%; animation-delay: 7s; }
        .particle:nth-child(9) { left: 90%; animation-delay: 8s; }

        /* Seção de exemplo para mostrar o scroll */
        .content-section {
            min-height: 100vh;
            background: white;
            padding: 4rem 2rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .content-box {
            max-width: 600px;
            text-align: center;
        }

        .content-title {
            font-size: 2.5rem;
            color: #1e293b;
            margin-bottom: 1rem;
            font-weight: 700;
        }

        .content-text {
            font-size: 1.2rem;
            color: #64748b;
            line-height: 1.6;
        }

        /* Animações */
        @keyframes nodeGlow {
            0%, 100% { 
                transform: scale(1); 
                box-shadow: 0 0 10px #00ff88; 
                opacity: 0.8;
            }
            50% { 
                transform: scale(1.4); 
                box-shadow: 0 0 30px #00ff88; 
                opacity: 1;
            }
        }

        @keyframes dataFlow {
            0% { opacity: 0; transform: scaleX(0); }
            50% { opacity: 0.6; transform: scaleX(1); }
            100% { opacity: 0; transform: scaleX(0); }
        }

        @keyframes gradientShift {
            0%, 100% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
        }

        @keyframes particleFloat {
            0% { 
                transform: translateY(100vh) rotate(0deg); 
                opacity: 0; 
            }
            10% { opacity: 0.3; }
            90% { opacity: 0.3; }
            100% { 
                transform: translateY(-100px) rotate(360deg); 
                opacity: 0; 
            }
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .neural-background {
                width: 300px;
                height: 300px;
                opacity: 0.1;
            }
            
            .hero-title {
                font-size: 2.5rem;
            }
            
            .hero-subtitle {
                font-size: 1.2rem;
            }
            
            .hero-description {
                font-size: 1rem;
            }
            
            .node {
                width: 12px;
                height: 12px;
            }
        }
    </style>
</head>
<body>
    <section class="hero-section">
        <!-- Logo Neural Network como Background -->
        <div class="neural-background">
            <div class="neural-network">
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                <div class="node"></div>
                
                <div class="connection conn-1"></div>
                <div class="connection conn-2"></div>
                <div class="connection conn-3"></div>
                <div class="connection conn-4"></div>
                <div class="connection conn-5"></div>
                <div class="connection conn-6"></div>
                <div class="connection conn-7"></div>
                <div class="connection conn-8"></div>
            </div>
        </div>

        <!-- Partículas flutuantes -->
        <div class="floating-particles">
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
            <div class="particle"></div>
        </div>

        <!-- Conteúdo do Hero -->
        <div class="hero-content">
            <h1 class="hero-title">AutomaDev</h1>
            <p class="hero-subtitle">Automação Inteligente para o Futuro</p>
            <p class="hero-description">
                Transformamos ideias em soluções automatizadas. 
                Desenvolvemos sistemas inteligentes que otimizam processos 
                e impulsionam o crescimento do seu negócio.
            </p>
            <button class="cta-button">Começar Agora</button>
        </div>
    </section>

    <!-- Seção adicional para demonstrar o scroll -->
    <section class="content-section">
        <div class="content-box">
            <h2 class="content-title">Próxima Seção</h2>
            <p class="content-text">
                Aqui você pode continuar com o resto do conteúdo do seu site. 
                A logo neural network fica apenas no hero section, criando um 
                impacto visual incrível no topo da página.
            </p>
        </div>
    </section>
</body>
</html>